# yatodo

Yet another todo list.

# Getting started

    $ npm install
    $ ./node_modules/.bin/grunt
    $ export mongoUrl=mongodb://mymongo
    $ export PORT=4001
    $ node ./app.js

Then surf to:

    http://localhost:4001/#/todos
